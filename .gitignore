.DS_Store

node_modules

public

.env

.iracing-tokens.json

# Everything under data/ is runtime state, not source: event results fetched from
# iRacing (initial sync on startup + POST /sync) and the events-include config
# written by POST /events. None of it belongs in the repo.
data/
